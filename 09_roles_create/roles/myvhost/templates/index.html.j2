# templetes폴더엔 동적파일
<html>
    <h1><center>Web Server</center></h1>
    <p>
        Hostname: {{ ansible_hostname }}            
        IP: {{ ansible_default_ipv4.address }}
        Web Directory: /var/www/vhosts/{{ ansible_hostname }}
    </p>

</html>

#고정이 아니라 변하는 파일이므로 j2 형식으로 쓰기.
# 내부 변수 활용하므로 동적변수여서 j2로!